# 🧭 คู่มือการย้ายเครื่องและกู้คืนโปรเจกต์ BizarreBig (Migration & Recovery Guide)

คู่มือนี้จัดทำขึ้นเพื่อช่วยให้คุณสามารถกู้คืนระบบหรือย้ายข้อมูลการพัฒนาโปรเจกต์ **BizarreBig** ไปยังคอมพิวเตอร์เครื่องใหม่ได้อย่างรวดเร็ว ในกรณีที่เปลี่ยนคอมพิวเตอร์ใหม่ หรือเกิดกรณีคอมพิวเตอร์เครื่องเดิมชำรุดเสียหาย

---

## ☁️ ทำไมข้อมูลของคุณถึงปลอดภัย?
1. **ซอร์สโค้ด (Source Code)**: ไฟล์โค้ดทั้งหมด (เช่น UI, คอมโพเนนต์, ดีไซน์ หน้าเว็บ) ได้ถูกบันทึกและอัปโหลดขึ้น **GitHub** ของคุณเรียบร้อยแล้วที่:
   `https://github.com/MrThak/BizarreBig`
2. **ฐานข้อมูลความคิดเห็น (Database)**: ข้อมูลคอมเมนต์ของผู้ชมทั้งหมดถูกเก็บอยู่บนเซิร์ฟเวอร์คลาวด์ของ **Supabase** ข้อมูลจึงไม่สูญหายแม้คอมพิวเตอร์จะพัง

---

## 🛠️ ขั้นตอนการย้ายเครื่อง (ทำตามลำดับนี้บนคอมพิวเตอร์เครื่องใหม่)

### ขั้นที่ 1: ติดตั้งโปรแกรมพื้นฐาน
ดาวน์โหลดและติดตั้งโปรแกรมเหล่านี้ในคอมพิวเตอร์เครื่องใหม่:
1. **[Git](https://git-scm.com/)** (สำหรับดาวน์โหลดโค้ด)
2. **[Node.js](https://nodejs.org/)** (แนะนำเวอร์ชัน LTS สำหรับรัน Next.js)
3. **[VS Code](https://code.visualstudio.com/)** (โปรแกรมเขียนโค้ดที่แนะนำ)

### ขั้นที่ 2: ดาวน์โหลดโค้ดจาก GitHub
เปิดโปรแกรม Terminal หรือ Command Prompt ในเครื่องใหม่ ไปยังโฟลเดอร์ที่ต้องการเก็บงาน แล้วรันคำสั่ง:
```bash
git clone https://github.com/MrThak/BizarreBig.git
```
*(ระบบจะสร้างโฟลเดอร์ `BizarreBig` ขึ้นมาในเครื่องใหม่พร้อมไฟล์โค้ดทั้งหมด)*

### ขั้นที่ 3: ติดตั้ง Dependencies
เปิดโปรแกรม Terminal เข้าไปในโฟลเดอร์โปรเจกต์ที่เพิ่งโหลดมา แล้วรันคำสั่งนี้เพื่อดาวน์โหลดไลบรารีที่จำเป็น:
```bash
cd BizarreBig
npm install
```

### ขั้นที่ 4: สร้างไฟล์เชื่อมต่อฐานข้อมูลใหม่ (.env.local)
เนื่องจากระบบไม่นำคีย์ความปลอดภัยส่วนตัวอัปโหลดขึ้น GitHub (เพื่อป้องกันผู้ไม่หวังดีขโมยคีย์) คุณจึงต้องสร้างไฟล์เชื่อมต่อใหม่ในเครื่องใหม่:
1. สร้างไฟล์ใหม่ชื่อ **`.env.local`** ไว้ในโฟลเดอร์หลักของโปรเจกต์ (โฟลเดอร์เดียวกับ `package.json`)
2. กรอกข้อมูลเชื่อมต่อ Supabase ของคุณลงไปดังนี้:
   ```env
   NEXT_PUBLIC_SUPABASE_URL=https://fieojbzfvbctjxnqjhxo.supabase.co
   NEXT_PUBLIC_SUPABASE_ANON_KEY=กรอกคีย์สาธารณะ (anon public key) ของคุณที่นี่
   ```
   *(คุณสามารถคัดลอกค่าทั้งสองนี้ได้จากแดชบอร์ดโครงการ Supabase ของคุณในเมนู Settings > API)*

### ขั้นที่ 5: เริ่มใช้งานระบบ
รันเซิร์ฟเวอร์ทดสอบบนเครื่องใหม่เพื่อทำงานต่อ:
```bash
npm run dev
```
และเปิดบราวเซอร์เข้าไปที่ลิงก์ **`http://localhost:3000`** ได้ทันที!

---

## 💾 คำแนะนำในการสำรองข้อมูลบน Google Drive (หรือคลาวด์อื่น ๆ)
* แนะนำให้คัดลอกเนื้อหาภายในไฟล์ **`.env.local`** (ค่า URL และ Key ของ Supabase) หรือก๊อปปี้ไฟล์ **`MIGRATION.md`** ตัวนี้ไปเก็บสำรองไว้บน Google Drive หรือ Password Manager ส่วนตัวของคุณ เพื่อความสะดวกรวดเร็วในการดึงข้อมูลมาเชื่อมต่อฐานข้อมูลใหม่โดยไม่ต้องเข้าหน้าแดชบอร์ด Supabase ครับ
