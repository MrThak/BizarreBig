# BizarreBig - แหล่งรวมข้อมูลหมวดหมู่เกมและอนิเมะระดับพรีเมียม

เว็บไซต์นี้เป็นระบบไดเรกทอรีจัดหมวดหมู่ แนะนำ และรีวิวเกมกับอนิเมะ ด้วยดีไซน์หรูหรา ธีมมืด (Dark Theme) ทันสมัย และเป็นมิตรต่อผู้ใช้งาน

---

## 🧭 ทิศทางและโครงสร้างของเว็บไซต์ (Project Direction)

1. **Category-Driven Hub**: มุ่งเน้นการค้นหาและเข้าถึงข้อมูลเกมและอนิเมะอย่างรวดเร็ว ผ่านระบบหมวดหมู่ที่ชัดเจน เช่น RPG, FPS, Action, Isekai, Shonen ฯลฯ
2. **Visual Premium Experience**: ใช้ดีไซน์กระจกฝ้า (Glassmorphism), สีสะท้อนแสง (Neon Glows - Violet/Indigo), และเอฟเฟกต์แอนิเมชันที่นุ่มนวล
3. **Developer/Gamer Spec Documentation**: มีส่วนคู่มือหรือค่าคอนฟิกเทคนิคการตั้งค่า (เช่น สเปกคอมพิวเตอร์ที่แนะนำ, รหัสสูตร, หรือคีย์ลัด) โดยใช้ **Shiki** ในการทำ Syntax Highlighting ให้โค้ดอ่านง่ายและสวยงามที่สุด
4. **Localization (ภาษาไทย)**: การแสดงผลและการโต้ตอบส่วนใหญ่รองรับภาษาไทยเป็นหลัก

---

## 🛠️ โครงสร้างเทคโนโลยีที่ใช้ (Tech Stack)

* **Framework**: Next.js (App Router) ด้วย TypeScript
* **Styling**: Tailwind CSS
* **Code Highlighting**: Shiki (ใช้สำหรับจัดรูปแบบคู่มือ/โค้ดในหน้าเว็บ)
* **Hosting / Development**: Node.js & npm

---

## 🚀 คู่มือการเพิ่ม/แก้ไขฟีเจอร์ (Feature Guide)

### 1. การเพิ่มเกมหรืออนิเมะใหม่
ข้อมูลเกมและอนิเมะทั้งหมดจะถูกจัดเก็บไว้ในไฟล์ข้อมูลแบบ Static ในโปรเจกต์:
* ไฟล์: `src/data/items.ts`
* วิธีการ: เพิ่มข้อมูลลงในอาร์เรย์ (Array) ตามโครงสร้างประเภทที่กำหนดไว้ (ดูตัวอย่างในโค้ด) ระบบค้นหาและระบบคัดกรองหมวดหมู่ในหน้าหลักจะดึงข้อมูลมาแสดงผลโดยอัตโนมัติ

### 2. การแก้ไขธีมและสี
* สามารถปรับแต่งพาเลทสีเพิ่มเติมได้ที่ `src/app/globals.css` และ `tailwind.config.ts` โดยปรับเปลี่ยนตัวแปร CSS เช่น `--background` หรือตัวแปรเฉดสีอื่น ๆ

### 3. การเพิ่มหน้าหมวดหมู่ใหม่
* ให้สร้างโฟลเดอร์เส้นทาง (Route) ใหม่ภายใต้ `src/app/` เช่น `src/app/games/page.tsx` หรือ `src/app/anime/page.tsx` ตามรูปแบบมาตรฐานของ Next.js App Router

---

## 📂 คู่มือการย้ายเส้นทางโปรเจกต์ในอนาคต (Migration Guide)

หากคุณต้องการย้ายโปรเจกต์ BizarreBig ไปยังโฟลเดอร์อื่นหรือไดรฟ์อื่นในอนาคต สามารถทำได้ตามขั้นตอนดังนี้:

1. **คัดลอกไฟล์ทั้งหมด (Copy/Move)**:
   * คัดลอกโฟลเดอร์ `BizarreBig` ไปยังเส้นทางใหม่ที่คุณต้องการ (เช่น ย้ายจากไดรฟ์อื่นมาที่ `F:\BizarreBig` หรือย้ายไปยังไดรฟ์อื่น ๆ)
   * *แนะนำ:* คุณสามารถข้ามโฟลเดอร์ `.next` และ `node_modules` ตอนคัดลอกได้เพื่อความรวดเร็ว

2. **ติดตั้ง Dependencies ใหม่**:
   * เปิด Terminal หรือ Command Prompt ในเส้นทางใหม่นั้น
   * รันคำสั่ง `npm install` เพื่อดึงข้อมูลและสร้างโฟลเดอร์ `node_modules` ใหม่ที่เข้ากับระบบ

3. **รันตัวพัฒนา (Dev Server)**:
   * รันคำสั่ง `npm run dev` เพื่อรันแอปพลิเคชันขึ้นมาใหม่
   * หน้าเว็บทดสอบจะเปิดขึ้นที่ [http://localhost:3000](http://localhost:3000)

4. **การตั้งค่า Workspace ใน IDE**:
   * หากใช้ตัวช่วยพัฒนา AI (เช่น Antigravity) ให้ทำการเปิดโฟลเดอร์เส้นทางใหม่นั้นเป็น Active Workspace เพื่อให้ AI เข้าถึงไฟล์ของโปรเจกต์ได้อย่างสมบูรณ์

---

## 🌐 คู่มือการตั้งค่าโดเมนและขึ้นระบบออนไลน์ (Domain & Deployment Guide)

ในขณะนี้โปรเจกต์ยังไม่ได้มีการซื้อหรือผูกโดเมนจริง แต่ได้เตรียมขั้นตอนและข้อมูลคอนฟิกสำหรับการดำเนินการในอนาคตไว้ดังนี้:

1. **การเลือกชื่อโดเมน (Domain Selection)**
   * ชื่อโดเมนที่แนะนำ: `bizarrebig.com` หรือชื่อที่เกี่ยวข้องกับตัวโปรเจกต์
   * แนะนำให้จดผ่านผู้ให้บริการที่รองรับ DNS Management ที่รวดเร็ว เช่น **Cloudflare**, **Namecheap** หรือผู้ให้บริการในไทย

2. **ขั้นตอนการขึ้นระบบและผูกโดเมนด้วย Vercel (Deployment via Vercel)**
   * **ขั้นตอนที่ 1**: นำ Source Code นี้ขึ้นระบบ Git (เช่น GitHub)
   * **ขั้นตอนที่ 2**: เข้าสู่ระบบ [Vercel](https://vercel.com/) และสั่ง Import Repository นี้เข้าไปเพื่อ Deploy ในฐานะโปรเจกต์ Next.js
   * **ขั้นตอนที่ 3**: ไปที่หน้าแดชบอร์ด Vercel -> เข้าไปที่โปรเจกต์ -> ไปที่ **Settings > Domains**
   * **ขั้นตอนที่ 4**: เพิ่มโดเมนของคุณลงไป (เช่น `bizarrebig.com`)
   * **ขั้นตอนที่ 5**: นำค่า DNS คอนฟิกที่ Vercel ระบุ ไปใส่ในระบบจัดการ DNS ของผู้ให้บริการจดโดเมนของคุณ:
     * สำหรับโดเมนหลัก (Apex Domain): ชี้ **A Record** ไปที่ IP `76.76.21.21`
     * สำหรับโดเมนย่อย (Subdomain เช่น `www`): ชี้ **CNAME Record** ไปที่ `cname.vercel-dns.com`

---

## 🔒 คู่มือการเชื่อมต่อฐานข้อมูล Supabase และระบบแอดมิน (Supabase & Admin Control Guide)

โปรเจกต์ได้รับการเชื่อมต่อกับระบบฐานข้อมูลคลาวด์ **Supabase** เพื่อบันทึกความคิดเห็นแบบเรียลไทม์ และระบบควบคุมสิทธิ์เฉพาะแอดมิน

1. **การตั้งค่าไฟล์สิ่งแวดล้อม (`.env.local`)**:
   คีย์สำหรับเรียกใช้งาน API และ URL ฐานข้อมูลจะถูกเก็บไว้ที่ไฟล์ `.env.local` ที่โฟลเดอร์ราก (Root) ของโปรเจกต์:
   * `NEXT_PUBLIC_SUPABASE_URL`: ที่อยู่ URL ของโปรเจกต์ Supabase
   * `NEXT_PUBLIC_SUPABASE_ANON_KEY`: คีย์สาธารณะสำหรับเชื่อมต่อ (anon public key)

2. **การตั้งค่าตารางฐานข้อมูล (SQL Schema)**:
   หากต้องการสร้างตารางใหม่บนแดชบอร์ด Supabase ให้คัดลอกโค้ด SQL จากไฟล์ **[supabase_schema.sql](file:///f:/BizarreBig/supabase_schema.sql)** ไปวางรันในเมนู **SQL Editor** ของ Supabase เพื่อสร้างตาราง `comments` โดยอัตโนมัติ

3. **ระบบสิทธิ์แอดมิน (Admin Only Access)**:
   * ส่วนแสดงสเปกเทคนิคและคอนฟิก (Shiki) ในหน้ารายละเอียดเกม/อนิเมะจะถูกจำกัดสิทธิ์ให้มองเห็นเฉพาะแอดมินเท่านั้น
   * ผู้ดูแลระบบสามารถเข้าสู่ระบบเพื่อปลดล็อกได้โดยคลิกปุ่ม **"เข้าสู่ระบบ 🔑"** ที่มุมขวาบน เพื่อใช้รหัสผ่าน หรือเข้าสู่ระบบด้วย Google
   * รหัสผ่านผู้ดูแลระบบแบบธรรมดาคือ **`admin`**
   * บัญชี Google ที่ได้รับสิทธิ์เป็นแอดมินโดยอัตโนมัติคือ **`thaksin819@gmail.com`** (สามารถแก้ไขหรือเพิ่มอีเมลแอดมินอื่น ๆ ได้ในภายหลังที่โค้ดไฟล์ `Navbar.tsx`)
   * สถานะสิทธิ์และความปลอดภัยของผู้ใช้จะอัปเดตแบบเรียลไทม์ในทุกส่วนผ่าน custom window events (`bizarre_auth_change`)

4. **การตั้งค่าระบบเข้าสู่ระบบด้วย Google OAuth (Google Login Setup Guide)**:
   การใช้งานระบบ Google Login จริงผ่าน Supabase จำเป็นต้องดึงการเชื่อมต่อจาก Google API ตามขั้นตอนต่อไปนี้:

   **ขั้นตอนที่ 1: การขอคีย์ความปลอดภัยจาก Google Cloud Console**
   1. ไปที่เว็บไซต์ [Google Cloud Console](https://console.cloud.google.com/) และลงชื่อเข้าใช้ด้วยบัญชี Google ของคุณ
   2. กดสร้างโปรเจกต์ใหม่ (New Project) และตั้งชื่อให้สอดคล้องกับแอปของคุณ
   3. เปิดแท็บเมนูซ้ายมือ ไปที่ **APIs & Services > OAuth consent screen** (หน้าจอยินยอม)
   4. เลือกประเภทผู้ใช้เป็น **External** แล้วกรอกข้อมูลพื้นฐานโครงการ เช่น ชื่อแอปพลิเคชัน และอีเมลติดต่อของผู้ดูแลระบบ
   5. ในขั้นตอนการเพิ่ม Scope ให้ตรวจสอบว่ามีการเพิ่มสิทธิ์เข้าถึงอีเมล (`/auth/userinfo.email`) และข้อมูลโปรไฟล์หลัก (`/auth/userinfo.profile`) เรียบร้อยแล้ว
   6. ไปที่เมนู **APIs & Services > Credentials** (ข้อมูลรับรอง) -> คลิก **+ Create Credentials > OAuth client ID**
   7. เลือกประเภทแอปพลิเคชันเป็น **Web application**
   8. ในหัวข้อ **Authorized redirect URIs** (URI การเปลี่ยนเส้นทางที่ได้รับอนุญาต) ให้นำลิงก์ Redirect URI จาก Supabase (ที่จะได้ในขั้นตอนที่ 2) มาวางลงในช่องนี้ เช่น:
      `https://fieojbzfvbctjxnqjhxo.supabase.co/auth/v1/callback`
   9. กด **Create** เพื่อรับรหัสคีย์ความปลอดภัย **Client ID** และ **Client Secret** มาเก็บไว้ใช้งาน

   **ขั้นตอนที่ 2: ตั้งค่าใช้งานบนแดชบอร์ดโครงการ Supabase**
   1. เข้าสู่หน้าแดชบอร์ดของ [Supabase](https://supabase.com/) และเลือกโครงการ `BizarreBig`
   2. ไปที่เมนู **Authentication** (ทางด้านซ้าย) -> ไปที่แถบ **Providers** -> ค้นหาหัวข้อผู้ให้บริการ **Google**
   3. เปิดสวิตช์เปิดใช้งาน (**Enabled**)
   4. นำคีย์ **Client ID** และ **Client Secret** ที่ได้จาก Google Cloud Console มากรอกลงในช่องข้อมูลให้ถูกต้อง
   5. ในหน้าตั้งค่านี้ Supabase จะแสดง **Callback URL (redirect URI)** ให้คัดลอกค่านี้ไปกรอกในฝั่งของ Google Cloud (ตามที่ระบุในขั้นตอนที่ 1 ข้อย่อยที่ 8)
   6. คลิกปุ่ม **Save** ด้านล่างสุด เพื่อเปิดระบบล็อกอิน Google สำหรับผู้ใช้งานทันที

